---
title: Help pages
output:
  html:
---

```{css, echo = FALSE}
h2 {
  text-align: center;
  margin-top: 2em;
}
```

```{r, include = FALSE}
litedown::reactor(fig.height = 5)
pkg_name = local({
    old <- setwd("../pkg")
    tryCatch(litedown:::detect_pkg(), finally = setwd(old))
})
```

`{r} litedown::pkg_manual(pkg_name, overview = FALSE)`
